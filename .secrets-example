# Docker version and container repo credentials
VERSION="2024.12.15.15"
CR_PAT=""

# Victron / VRM / MQTT configuration
CERBOGX_IP="192.168.x.x"
MOSQUITTO_IP="mosquitto.mydomain.net" # Possible and most likely the same as cerbo IP
VRM_LOGIN_URL='https://vrmapi.victronenergy.com/v2/auth/login'
VRM_API_URL="https://vrmapi.victronenergy.com/v2"
VRM_PORTAL_ID=''
VRM_SITE_ID=""
# Personal access token (preferred). Victron deprecated username/password auth on
# 2026-06-01. Generate at VRM Portal > Preferences > Access Tokens and paste the
# RAW token here (do NOT include the word "Token" — the code adds the
# "X-Authorization: Token <token>" header itself). When set, VRM_USER/VRM_PASS
# are ignored.
VRM_API_TOKEN=""
# Legacy username/password (deprecated — used only when VRM_API_TOKEN is empty).
VRM_USER=""
VRM_PASS=""

# Domoticz
DZ_URL_PREFIX="http://your_server_host:8080/json.htm?type=command&param=udevice&idx="

# tesla credentials and home address LAT and LONG
TESLA_EMAIL=""
HOME_ADDRESS_LAT=50.0000
HOME_ADDRESS_LONG=4.0000

# Tesla Fleet API (register an app at developer.tesla.com)
TESLA_FLEET_CLIENT_ID=""
TESLA_FLEET_CLIENT_SECRET=""
TESLA_FLEET_DOMAIN=""
TESLA_FLEET_REDIRECT_URI=""
# Path to the EC private key used to sign Fleet API command requests
TESLA_FLEET_PRIVATE_KEY_PATH="secrets/tesla-fleet/private-key.pem"
# Filled in after completing the OAuth authorization-code exchange. Both tokens are
# updated atomically when Tesla refreshes/rotates them, so this file must be writable
# by the controller process. Never copy an older refresh token over the current one.
TESLA_FLEET_ACCESS_TOKEN=""
TESLA_FLEET_REFRESH_TOKEN=""
TESLA_FLEET_API_BASE_URL="https://fleet-api.prd.eu.vn.cloud.tesla.com"
TESLA_FLEET_VEHICLE_ID=""

# Tibber options
TIBBER_ACCESS_TOKEN=""
HOME_ID="" # Optional: set to a specific Tibber home ID for live measurements

# Pushover API Access
PO_USER_ID=""
PO_API_KEY=""

# AWS Credentials
AWS_ACCESS_KEY=""
AWS_SECRET_KEY=""

# HomeConnect options
CLIENTID=""
CLIENTSECRET=""
REDIRECTURI=""

# OpenWeatherMap Data
# OWM_APIKEY=
# LAT=
# LON=

# --- AI Advisor (read-only dashboard review) -------------------------------
# RECOMMENDED: point the advisor at a SUBSCRIPTION-LOGIN CLI via ADVISOR_CLI_CMD in
# .env (e.g. `gemini -p {prompt}` after `gemini login`, or `codex exec {prompt}`),
# so usage draws from your existing plan with no API key and no per-call charge.
# The two tokens below are only for the Claude-specific auth paths:
#   CLAUDE_CODE_OAUTH_TOKEN — Claude Pro/Max via Claude Code (`claude setup-token`).
CLAUDE_CODE_OAUTH_TOKEN=""
#   ANTHROPIC_API_KEY — pay-as-you-go fallback (used only when ADVISOR_AUTH=api).
ANTHROPIC_API_KEY=""
